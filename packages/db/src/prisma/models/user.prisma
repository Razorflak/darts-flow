
model User {
  id        String   @id @default(cuid())
  firstName String
  lastName  String
  userName  String
  password  String
  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt
}
